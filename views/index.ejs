<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Games</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        h1 {
            color: #333;
        }
        ul {
            list-style-type: none;
            padding: 0;
            width: 50%;
            height: auto;
        }
        li {
            background: #fff;
            margin: 10px 0;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            width: 100%;
            text-align: center;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }
        strong {
            margin-bottom: 5px;
            text-align: center;
            width: auto;
        }
        button {
            background-color: #007BFF;
            color: white;
            border: none;
            padding: 10px 15px;
            border-radius: 5px;
            cursor: pointer;
            margin-right: 5px;
            display: flex;
            justify-content: center;
            width: 50%;
        }
        button:hover {
            background-color: #0056b3;
        }
        form {
            width: 100%;
            display: flex;
            justify-content: center;
            margin-top: 10px;
        }
        a {
            text-decoration: none;
            color: #007BFF;
            margin-top: 20px;
        }
        a:hover {
            color: #0056b3;
        }
    </style>
</head>
<body>
    <h1>Games List</h1>
    <ul>
        <% games.forEach(game => { %>
            <li>
                <strong>Title:</strong> <%= game.title %> <br>
                <strong>Price:</strong> <%= game.price %> <br>
                <strong>Rating:</strong> <%= game.rating %>
                <button onclick="window.location.href='/edit/<%= game.id %>'">Edit</button>
                <form action="/delete/<%= game.id %>" method="post">
                    <input type="hidden" name="id" value="<%= game.id %>">
                <button onclick="window.location.href='/delete/<%= game.id %>'">Delete</button>
                </form>
            </li>
        <% }) %>
    </ul>
    <a href="/create">Add a new game</a>
</body>
</html>